specVersion: "v1"
serviceId: "listcolissage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "listcolissage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
